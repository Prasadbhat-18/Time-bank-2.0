# Client (Vite) environment variables
# Rename to .env.local and fill with your Firebase + Cloudinary config

VITE_FIREBASE_API_KEY=your_firebase_api_key
VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id

## Cloudinary (unsigned uploads)
# Only cloud_name and upload_preset are required for unsigned uploads in the browser.
VITE_CLOUDINARY_CLOUD_NAME=your_cloud_name
VITE_CLOUDINARY_UPLOAD_PRESET=your_unsigned_preset

## Optional: Local server URL for OTP + AI mediator
# Defaults to http://localhost:4000 if unset
# VITE_SERVER_URL=http://localhost:4000

## Optional: Max number of images allowed per service
# VITE_MAX_SERVICE_IMAGES=10
